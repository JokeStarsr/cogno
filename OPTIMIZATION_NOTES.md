# Cogno Reader 优化记录（2026-08-17）

本轮优化的三条主线，对应产品面临的三个真实约束：

1. **Token 经济性** —— AI 走 sub2api（阿里云 Token Plan 包月，deepseek-v4-flash-0731 映射），
   要求"**token 用尽也能撑住**"：错误可识别、对话可降级、调用不浪费。
2. **性能** —— 眼动信号 2s 一轮驱动全页重渲染的架构先天昂贵，逐一拆掉。
3. **体验** —— 阅读场景的连续性（进度不丢、上下文精准、小屏可用）。

---

## 一、Token 经济性（用户点名：按"用完的标准"优化）

### 1.1 AI 失败分类与可操作提示 `src/lib/llm.ts`
- 新增 `classifyFailure()`：按 HTTP 状态码 + 错误文本识别 **余额不足 / 配额用尽 / 限流 / Key 无效 / 服务端故障 / 网络错误**。
- 覆盖 sub2api 的两大已知坑：`INSUFFICIENT_BALANCE`、`No available accounts`（见 sub2api 故障记忆）。
- 新增 `friendlyFailure()`：余额不足/限流时给出明确中文指引 + 自动降级，不再是甩一段原始错误。

### 1.2 本地降级问答引擎（新模块）`src/lib/localAgent.ts`
- **AI 不可用 / 未配置 / 余额不足 / 限流时，四代理退化为基于知识图谱的本地苏格拉底话术**，对话不断裂。
- 澄清者/挑战者/连接者/拓展者各有话术模板：引用概念描述、前置、同级类比、后继延伸。
- 匹配算法：**2-gram 重叠评分**——"动态规划到底怎么用" 也能命中 "动态规划基础"；"今晚吃什么" 不会误命中。
- 每代理每次会话最多本地作答 2 轮（防模板复读），用尽后提示配置 AI；手动对话时自动带上本地兜底。
- 本地回答在 UI 上标注「本地」徽标，透明可辨。

### 1.3 调用瘦身（每次 API 请求都更省）
- **多轮历史预算化**：`trimHistory()` 从"固定 8 条"改为**字符预算（1600）裁剪**，优先保留最近的追问。
- **阅读上下文预算化**：`trimContext()` 只把"开头主题句 + 尾部当前阅读位置"发给 AI，中间省略（500 字符预算）。
- **上下文精准化**：`TextViewer.visibleText()` 从"全文尾部 600 字"改为**返回视口中心线附近的段落（±1 段）**——AI 拿到的是用户正在看的内容，而不是文末（见 2.2）。
- **旧缓存保留**：同代理 + 同问题缓存命中直接复用（不重复计费）。

### 1.4 设置页「测试连接」
- 最小请求（max_tokens=8，约 10 token）验证 Base URL / Key / 模型，**识别"余额不足/无可用账户/限流"**，用户在阅读前就知道后端状态。

---

## 二、性能

### 2.1 App 三页保持挂载（架构级）`src/App.tsx`
- 原来切换视图会**卸载 ReaderPage**：阅读进度、AI 对话、眼动追踪全丢。
- 现改为三页全部保持挂载（CSS 显隐），**阅读中途去设置再回来，正文、滚动位置、AI 对话原样还在**。
- 配套：`ReaderPage` 在非阅读视图时暂停认知循环、日志写入与触发检查（不空耗 token/IndexedDB）。

### 2.2 TextViewer 重写 `src/components/Reader/TextViewer.tsx`
- **匹配器重写**：旧实现是"词元 × 概念"双重循环 + `label.includes(token)` 粗匹配，
  **会把"复杂度"标成"时间复杂度"、"时间"也标错**，且每 2s 认知 tick 都全量重算。
  新实现：标签长度降序做正则交替、一次 `matchAll` 全文本扫描，**天然最长优先、零错标**，且挂在 `useMemo` 下（文本不变零重算）。
- **概念识别并入渲染通道**：高亮勾类时同时收集命中概念，删掉 MutationObserver + `innerText` 全量扫描（少一次 DOM 读 + 一个 Observer）。
- **段落级渲染**：正文按空行分段为 `<p>`（阅读呼吸感 + 视口锚定上下文的基础）。
- **概念浮层**：点击高亮概念弹出卡片（描述 + 前置概念），用全局单例 div，零依赖。
- **滚动即时翻页检测**：新增 `onVirtualScroll`，文本模式翻页速率/页停留从"3s 轮询"变为**滚动即时生效**。

### 2.3 2s 认知 tick 的渲染隔离
- `CognitiveStateRing`、`AgentPanel`、`PdfViewer` 全部 `memo()`；`TextViewer` 的 token 树在 `useMemo` 下——
  2s 一次的状态更新不再重渲染整棵阅读树。

### 2.4 PDF 视口虚拟化 `src/components/Reader/PdfViewer.tsx`
- **原来整本 PDF 一页不落地全渲染 canvas**（几百页 = 几百张位图）。
- 现在只渲染**视口中心页 ±2 页**；离开缓冲范围立即清空画布释放 GPU 内存；wrapper 高度用 viewport 预计算，清空后布局不塌陷。
- 缩放/窗口尺寸变化自动重算可视范围。

### 2.5 Cytoscape 懒加载 `src/components/KnowledgeGrid/GridCanvas.tsx`
- 知识网格从静态 import 改为**抽屉打开才动态加载**（~440KB chunk 独立分包，构建产物显式可见）。

### 2.6 IndexedDB 写入优化 `src/lib/storage.ts`
- 认知日志清理从"每 2s 一次删除事务（带全表扫描）"改为 **60s 批量清理**。
- 无阅读源（读数据为空）时不写日志；不在阅读视图时不写日志。

---

## 三、产品体验

- **首次使用三步引导卡片**（概览页，可关闭，localStorage 记忆）：配置 AI → 开始阅读 → 思考被推动。
- **设置页**：端点测试按钮、数据清空范围补齐（含已保存文档）。
- **「粘贴网页」诚实化**：真实尝试抓取正文（成功则导入正文），跨域失败明确提示并回退为 URL 文本导入。
- **Dashboard**：今日阅读按**本地日期**统计（修掉 UTC 时区错算）；切回概览自动刷新数据。
- **AgentPanel**：轻量 Markdown 渲染（代码块/粗体/行内代码/段落），本地降级应答显示「本地」徽标。
- **小屏适配（≤720px）**：状态环变右下角悬浮徽标（不挤内容区）、知识抽屉全屏、代理面板自适应，顶部导航紧凑。

---

## 技术栈变化

无新增依赖。全部优化均用现有框架能力（React memo/useMemo、CSS 媒体查询、正则引擎、IndexedDB）实现。

## 验证

- `npm run build`（tsc + vite）通过；cytoscape 独立成懒加载 chunk。
- Edge 无头渲染冒烟：三页挂载、引导卡、状态环渲染正常，无渲染错误。
- 概念匹配器正确性（node 实测）：最长优先、无重复、不产生部分标签。
- 本地降级引擎（node 实测）：口语化输入 5/5 命中，无关输入不误命中，话术预算语义正确。